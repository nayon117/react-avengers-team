 
import PropTypes from 'prop-types';

const Cart = () => {
    return (
        <div>
            <h2>This is cart</h2>
        </div>
    );
};

Cart.propTypes = {
    Cart:PropTypes.func.isRequired
};

export default Cart;